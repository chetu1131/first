<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />

    <title>Login System in Node.js Express</title>
  </head>
  <style>
    #forgot {
      text-decoration: none;
      background-color: red;
      color: black;
      padding: 9px;
      border-radius: 6px;
    }
  </style>
  <body>
    <div class="container">
      <h1 class="mt-5 mb-5 text-center text-primary">
        <b>Login System in Node.js Express</b>
      </h1>

      <form action="" name="login" id="login" autocomplete="on">
        <div class="row mt-5">
          <div class="col-md-3">&nbsp;</div>
          <div class="col-md-6">
            <h1>Hi User, Welcome</h1>

            <!-- <a href="/logout" class="btn btn-primary">Logout</a> -->

            <div class="card">
              <div class="card-header">Login</div>
              <div class="card-body">
                <form method="post" action="/login">
                  <div class="mb-3">
                    <label>Email Address</label>
                    <input type="email" name="email" class="form-control" />
                  </div>
                  <div class="mb-3">
                    <label>Password</label>
                    <input
                      type="password"
                      name="password"
                      class="form-control"
                    />
                  </div>
                  <div class="mb-3">
                    <input
                      name="submit"
                      id="submit"
                      type="button"
                      class="btn btn-primary"
                      value="Login"
                      onclick="return simple()"
                    />
                    <a id="forgot" href="/forgotpass">forgot Password</a>
                  </div>

                  <div>
                    Register if not
                    <a href="/">Register</a>
                  </div>
                </form>
              </div>
            </div>
          </div>
        </div>
      </form>
    </div>
  </body>
  <script>
    async function login_post() {
      console.log("hello");
      var obj = new URLSearchParams(new FormData(login));
      console.log(obj);
      let result = await fetch("http://localhost:8000/login", {
        method: "POST",
        headers: { "Content-Type": "application/x-www-form-urlencoded" },
        body: obj,
      });

      result = await result.json();
      console.log(result);

      if (result.msg == "login") {
        window.location.replace("http://localhost:8000/home");
      } else if (result.msg == "invalid") {
        window.location.replace("http://localhost:8000/fail");
      }
    }

    function simple() {
      login_post();
    }
    let data;
    async function forgotpass() {
      data = await result.json();
      console.log(result.email);
    }
  </script>
</html>
